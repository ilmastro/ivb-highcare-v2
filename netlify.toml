# Netlify configuration

[build]
  publish = "."

# SPA fallback — all paths serve index.html
# Hash fragments (#invite_token etc) never reach the server,
# so they are handled client-side by the identity widget in admin/index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
